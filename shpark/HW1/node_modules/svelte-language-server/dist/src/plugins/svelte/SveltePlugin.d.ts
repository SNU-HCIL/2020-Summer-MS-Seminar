import { CodeAction, CodeActionContext, CompletionList, Diagnostic, Hover, Position, Range, TextEdit, WorkspaceEdit } from 'vscode-languageserver';
import { Document } from '../../lib/documents';
import { LSConfigManager } from '../../ls-config';
import { CodeActionsProvider, CompletionsProvider, DiagnosticsProvider, FormattingProvider, HoverProvider } from '../interfaces';
import { SvelteCompileResult } from './SvelteDocument';
export declare class SveltePlugin implements DiagnosticsProvider, FormattingProvider, CompletionsProvider, HoverProvider, CodeActionsProvider {
    private configManager;
    private prettierConfig;
    private docManager;
    private cosmiConfigExplorer;
    constructor(configManager: LSConfigManager, prettierConfig: any);
    getDiagnostics(document: Document): Promise<Diagnostic[]>;
    getCompiledResult(document: Document): Promise<SvelteCompileResult | null>;
    formatDocument(document: Document): Promise<TextEdit[]>;
    getCompletions(document: Document, position: Position): Promise<CompletionList | null>;
    doHover(document: Document, position: Position): Promise<Hover | null>;
    getCodeActions(document: Document, range: Range, context: CodeActionContext): Promise<CodeAction[]>;
    executeCommand(document: Document, command: string, args?: any[]): Promise<WorkspaceEdit | string | null>;
    private featureEnabled;
    private getSvelteDoc;
}
