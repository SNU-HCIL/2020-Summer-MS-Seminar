import { DocumentSnapshot, SvelteSnapshotOptions } from './DocumentSnapshot';
export declare class SnapshotManager {
    private projectFiles;
    private documents;
    private lastLogged;
    constructor(projectFiles: string[]);
    updateByFileName(fileName: string, options: SvelteSnapshotOptions): void;
    has(fileName: string): boolean;
    set(fileName: string, snapshot: DocumentSnapshot): Map<string, DocumentSnapshot>;
    get(fileName: string): DocumentSnapshot | undefined;
    delete(fileName: string): boolean;
    getFileNames(): string[];
    getProjectFileNames(): string[];
    private logStatistics;
}
